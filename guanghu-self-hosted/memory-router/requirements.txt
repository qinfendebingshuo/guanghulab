# ============================================================
# requirements.txt · Memory Router Agent 依赖清单
# HLDP-ARCH-001 [L3] · Phase-0-005
# 工单: YD-A05-20260425-003
# Python: 3.11+
# ============================================================

# ── PostgreSQL 异步驱动 ──
asyncpg>=0.29.0

# ── pgvector Python 绑定 ──
pgvector>=0.3.0

# ── Web 框架 (API 层) ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── 数据校验 ──
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── YAML 配置解析 ──
PyYAML>=6.0.1

# ── HTTP 客户端 (Embedding API 调用) ──
httpx>=0.27.0

# ── OpenAI Embedding (可选 · 生产环境按需替换) ──
openai>=1.30.0

# ── 日志 ──
python-json-logger>=2.0.7
